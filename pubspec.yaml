name: toss_invest_api
description: "Unofficial package for Toss Invest Open API."
version: 0.1.0-dev.1
environment:
  sdk: ^3.13.3
  flutter: ">=1.17.0"

##################################################

dependencies:
  flutter:
    sdk: flutter

  # data
  http: ^1.6.0
  web_socket_channel: ^3.0.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # serialize
  build_runner: ^2.16.0
  freezed: ^4.0.0
  json_serializable: ^6.14.0